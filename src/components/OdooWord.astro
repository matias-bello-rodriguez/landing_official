---
interface Props {
  tone?: "brand" | "on-dark";
  class?: string;
}

const { tone = "brand", class: className } = Astro.props;
---

<img
  class:list={["odoo-word", tone === "on-dark" && "odoo-word--on-dark", className]}
  src="/assets/odoo-logo.svg"
  alt="Odoo"
  width="126"
  height="40"
/>
